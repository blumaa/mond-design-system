// @mond-design-system/react — public barrel.
// Components appear here as phases land. Phase 0 exports nothing but the
// shared class-name helper so the package builds and its pipeline is proven.
export { cx } from "./internal/cx";
export type { CSSVars } from "./internal/cx";
