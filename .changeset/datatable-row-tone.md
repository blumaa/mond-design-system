---
"@mond-design-system/react": minor
---

`DataTable` takes `rowTone`, which tints a whole row `danger`, `warning` or `success`. For a row that needs attention, where a badge in one cell is easy to miss. The tint is colour only, so say why in a cell too.
