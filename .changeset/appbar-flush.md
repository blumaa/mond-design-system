---
"@mond-design-system/react": minor
---

AppBar `flush` prop: drops the page padding so bar content reaches the
screen edge. Off by default — the padding is what lines the bar up with
Container's page column.
