---
"@mond-design-system/tokens": minor
---

Add the `--mds-type-code` type role: mono, one step above prose. A run a reader has to copy character by character — a one-time password, a key, an identifier — reads differently from a sentence, and until now an app had no role to ask for it and had to reach for a raw scale step.
