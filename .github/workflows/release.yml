name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write
  packages: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
  
  release:
    runs-on: ubuntu-latest
    needs: ci
    environment: npm-publishing
    outputs:
      released: ${{ steps.semantic.outputs.new_release_published }}
      version: ${{ steps.semantic.outputs.new_release_version }}
      
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build packages
        run: yarn build

      - name: Run tests
        run: yarn test
        working-directory: component-lib

      - name: Run lint
        run: yarn lint
        working-directory: component-lib

      - name: Type check
        run: yarn typecheck
        working-directory: component-lib

      - name: Test npm authentication
        run: |
          echo "Testing npm whoami with provided token..."
          echo "Setting up .npmrc with NPM_TOKEN..."
          echo "Token length: ${#NPM_TOKEN}"
          echo "Token starts with: ${NPM_TOKEN:0:10}..."
          echo "NPM config file location: $NPM_CONFIG_USERCONFIG"
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > $NPM_CONFIG_USERCONFIG
          cd component-lib
          npm whoami || echo "npm whoami failed"
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Semantic Release
        id: semantic
        run: |
          echo "Setting up npm authentication for semantic-release..."
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > $NPM_CONFIG_USERCONFIG
          cd component-lib
          npx semantic-release --debug
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true